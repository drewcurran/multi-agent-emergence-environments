hide_and_seek_quadrant.jsonnet:
Dict(
    agent_qpos_qvel:Box(3, 10),
    box_obs:Box(2, 12),
    lidar:Box(30, 1),
    mask_aa_obs:Box(3,),
    mask_ab_obs:Box(2,),
    mask_ab_obs_spoof:Box(2,),
    mask_ar_obs:Box(1,),
    observation_self:Box(10,),
    ramp_obs:Box(1, 9)
)

capture_the_flag.py:

playground = 0, mode = 0
Dict(
    agent_qpos_qvel:Box(3, 8), # No dimensions for hider, prep_obs
    box_obs:Box(2, 12),
    flag_obs:Box(2, 15), # New observation key for flags
    lidar:Box(30, 1),
    mask_aa_obs:Box(3,),
    mask_ab_obs:Box(2,),
    mask_ab_obs_spoof:Box(2,),
    mask_af_obs:Box(2,), # New observation key for flags
    mask_ar_obs:Box(1,),
    observation_self:Box(8,), # No dimensions for hider, prep_obs
    ramp_obs:Box(1, 9)
)